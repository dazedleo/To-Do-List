# To-Do-List
Provides secure user authentication and RESTful APIs to create, view, update, and delete to-do tasks. Uses serializers for validation, Django ORM for data storage, and ensures users can only manage their own tasks with filtering and search support.
